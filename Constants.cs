public static class Constants
{
    /// <summary>
    /// Path that points to .todos file.
    /// </summary>
    public static string TodoFilePath = "/home/andrew/.todos";

    /// <summary>
    /// Put at the beginning of a todo item to mark it as completed.
    /// </summary>
    public static string CompleteMarker = "_^";

    /// <summary>
    /// Used to demarcate the completion timestamp.
    /// </summary>
    public static string Timestamp = "&*";
}